machine:
  environment:
    CONTAINER_NAME: quay.io/redsift/sandbox-go-rocksdb
    GO_V: 1.9.4
    ROCKSDB_V: 5.8
  pre:
    - sudo curl -L -o /usr/bin/docker 'https://s3-external-1.amazonaws.com/circle-downloads/docker-1.9.0-circleci'
    - sudo chmod 0755 /usr/bin/docker
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --build-arg ROCKSDB_VERSION=$ROCKSDB_V -t $GO_V .

test:
  override:
    - docker run $GO_V -c "ls -l /usr/local/lib"
    - docker run $GO_V -c "go get github.com/redsift/gorocksdb"

deployment:
  production:
    branch: master
    commands:
      - docker login -e $QUAY_EMAIL -u $QUAY_USER -p $QUAY_PASS quay.io
      - |
        set -e
        echo "Tagging as $CONTAINER_NAME:v$ROCKSDB_V"
        docker tag -f $GO_V:latest $CONTAINER_NAME:v$ROCKSDB_V
        latest=$GO_V:latest
        echo "Tagging $latest as $CONTAINER_NAME:latest"
        docker tag $latest $CONTAINER_NAME:latest
        docker push $CONTAINER_NAME
